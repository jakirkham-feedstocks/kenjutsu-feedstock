{% set name = "kenjutsu" %}
{% set version = "0.4.0" %}
{% set sha256 = "81b36a9fa52d031cfa6b78e4d6a156232c258dd44854cfa0c86b34f8373a745d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - kenjutsu

about:
  home: https://github.com/jakirkham/kenjutsu
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: Python utility functions for slices.
  doc_url: https://kenjutsu.readthedocs.io/
  dev_url: https://github.com/jakirkham/kenjutsu

extra:
  recipe-maintainers:
    - jakirkham
